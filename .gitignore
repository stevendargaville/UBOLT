# Build artifacts
*.o
*.mod
lib/

# Compiled test executables (every driver is a Kokkos TU, so the binary ends in k)
tests/*k
!tests/*.cxx
!tests/*.hpp

# Scalar flux VTK output (-flux_vtk is opt-in on the drivers; no recipe writes one)
*.vts
*.vtr

# Old root-level executable (pre-Phase-0 layout)
UBOLTk

# Editor
.vscode/
